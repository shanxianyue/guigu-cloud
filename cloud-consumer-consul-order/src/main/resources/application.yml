server:
  port: 9001
spring:
  application:
    name: consul-consumer-order
  cloud:
    consul:
      host: localhost
      port: 8500
      enabled: true
      discovery:
        service-name: ${spring.application.name}
